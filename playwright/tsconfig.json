{
  // Shares the strict baseline with the rest of the workspace.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["node"],
    // migration-gate.ts reads the pinned CLI version from ../config.js
    "allowJs": true
  },
  "include": ["**/*", "../playwright.config.ts"]
}
